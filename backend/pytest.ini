[pytest]
# 1. 只收集 test 目录下的文件
testpaths = test

# 2. 绝对禁止递归进入 app 目录 (解决 author.py 冲突和 main.py 导入错误)
norecursedirs = app venv .git node_modules

# 3. 异步配置
asyncio_mode = auto

# 4. 文件匹配规则
python_files = test_*.py
python_classes = Test*
python_functions = test_*